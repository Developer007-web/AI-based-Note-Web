cd path/to/my-notes-app   # folder that contains app.js, package.json, etc.
# 1. Initialize git repo (only once)
git init

# 2. Add everything
git add .

# 3. Commit
git commit -m "first commit - AI notes app with Express + Gemini + OpenAI"

# 4. Connect to GitHub
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/my-notes-app.git

# 5. Push
git push -u origin main
https://advanced-notes-app.netlify.app/
git add .
git commit -m "describe change"
git push
